framework:
    cache:
        # Use Redis as the default application cache when REDIS_URL is set.
        # Falls back to filesystem in environments without Redis.
        app: cache.adapter.redis
        default_redis_provider: '%env(REDIS_URL)%'

        # Doctrine metadata / query caches
        system: cache.adapter.system
